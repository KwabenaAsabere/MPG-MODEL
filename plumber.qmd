---
title: "MPG MODEL PLUMBER API"
format: html
editor: visual
---

```{r}
library(plumber)
library(vetiver)
library(pins)
library(tidymodels)
library(ranger)

board <- board_folder("model")

model <- vetiver_pin_read(board, "car-mpg-model")

pr() |>
  vetiver_api(model)

```
